#include<stdio.h>

void sum_avg(int*,int*);

int main()
{
    int sum;
    float avg;
    sum_avg(&sum,&avg);
    printf("Sum and average is %d, %f ",sum,avg);
    return 0;
}

void sum_avg(int* sum,int* avg)
{
    int i;
    printf("Enter total no. of numbers: ");

}
